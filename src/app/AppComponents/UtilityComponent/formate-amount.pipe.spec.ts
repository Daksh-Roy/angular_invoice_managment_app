import { FormateAmountPipe } from './formate-amount.pipe';

describe('FormateAmountPipe', () => {
  it('create an instance', () => {
    const pipe = new FormateAmountPipe();
    expect(pipe).toBeTruthy();
  });
});
